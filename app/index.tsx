import { StyleSheet, Text, View } from 'react-native';

const Page = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontFamily: 'DMSans_700Bold' }}>Page</Text>
    </View>
  );
};

export default Page;

const styles = StyleSheet.create({});
